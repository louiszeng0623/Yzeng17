name: Update Calendar Automatically

on:
  schedule:
    # 每天北京时间 04:00 自动执行（UTC 时间 20:00）
    - cron: '0 20 * * *'
  workflow_dispatch:   # 允许你在 GitHub Actions 手动触发运行

permissions:
  contents: write      # 允许自动推送到仓库

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 pytz

      - name: Fetch latest schedules from 懂球帝 & 直播吧
        run: python scripts/fetch_schedule.py

      - name: Build calendar (.ics)
        run: python scripts/build_ics.py

      - name: Commit and push updates
        run: |
          git config user.name "github-actions"
          git config user.email "actions@users.noreply.github.com"
          git add data/*.csv calendar.ics
          if git diff --cached --quiet; then
            echo "✅ 没有新变化，无需提交。"
          else
            git commit -m "🤖 自动更新赛程与日历 ($(date '+%Y-%m-%d %H:%M:%S'))"
            git push
          fi
