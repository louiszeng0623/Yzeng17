name: Update Calendar (Stable API Version)

on:
  schedule:
    - cron: '0 20 * * *'   # 每天凌晨 4:00（北京时间）
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: pip install requests
      - name: Fetch new schedules
        run: python scripts/fetch_schedule.py
      - name: Build calendar
        run: python scripts/build_ics.py
      - name: Commit and push updates
        run: |
          git config user.name 'github-actions'
          git config user.email 'actions@users.noreply.github.com'
          git add data/ calendar.ics
          if git diff --cached --quiet; then
            echo "📭 无需更新"
          else
            git commit -m "自动更新赛程与日历"
            git push
          fi
